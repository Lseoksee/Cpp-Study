나무위키 참고: https://namu.wiki/w/C%2B%2B/%EB%AC%B8%EB%B2%95

c++에서 구조체 왜씀?: https://velog.io/@thekim12/%ED%81%B4%EB%9E%98%EC%8A%A4%EB%A7%8C-%EC%8D%A8%EB%8F%84-%EB%90%98%EB%8A%94%EA%B1%B0%EC%95%84%EB%83%90-%EA%B5%AC%EC%A1%B0%EC%B2%B4-%EC%99%9C%EC%94%80

강좌 볼만한거: https://tcpschool.com/cpp/cpp_intro_basic

C++ 코어 가이드 라인:

- [한글 가이드라인](https://cppkorea.org/CppCoreGuidelines/Class/)
- [영문 가이드라인 원본](https://github.com/isocpp/CppCoreGuidelines)

## char 와 wchar_t 차이

### char

크기: 보통 1바이트 (8비트)
하나의 바이트로 표현 가능한 문자를 저장
한글, 한자 같은 멀티바이트 문자는 여러 개의 char를 이어서(UTF-8 인코딩 등) 표현해야 함

유니코드를 지원하기 위해 한글같은 문자가 들어오면 크기를 유동적으로 조절함
단순 저장-출력은 문제가 없고, 오히려 이 경우에는 wchar_t보다 char를 쓰는게 맞음

근데 이게 문제는 문자열을 처리를 해야되는경우 char는 적합하지 않음
예를 들어 `안녕하세요` 라는 문자열에 길이를 `length()`를 값을 확인해보면 15정도가 나옴, char가 유니코드를 지원하기 위해 바이트를 늘려버려서임

심저어는 `안`을 불러오기 위해 `str[0]`를 호출하면 `안`이 아니라 `안`의 첫 바이트만 불러오게 됨, 즉 문자열 처리를 할때는 char는 적합하지 않음

### wchar_t

크기: 플랫폼마다 다름 (표준에서 크기를 명시하지 않음)
Windows: 보통 2바이트 (UTF-16 기반)
Linux/macOS: 보통 4바이트 (UTF-32 기반)

바이트 해석을 위해 `setlocale(LC_ALL, "<로케일>");`을 호출해야함

char와 달리 wchar_t는 멀티바이트 문자를 하나의 wchar_t로 표현할 수 있음

## 가장 대중적인 c++ 표준

> c++17임

아래는 2023년 기준 설문

https://blog.jetbrains.com/clion/2024/01/the-cpp-ecosystem-in-2023/

## 소멸자 호출

- `데이터(data)`: 영역 해당 프로그램이 종료될 때
- `스택(stack)`: 영역 해당 객체가 정의된 블록을 벗어날 때
- `힙(heap)`: 영역 delete를 사용하여 해당 객체의 메모리를 반환할 때
- 임시 객체 임시 객체의 사용을 마쳤을 때

## 클래스 오퍼래이션 함수 재약사항

https://tcpschool.com/cpp/cpp_operatorOverloading_constraint

## 왜 해더파일과 소스파일을 분리하는가?

> 그냥 소스파일 include 방식에 문제점

### 컴파일 과정

소스파일을 그냥 include한경우
컴파일러가 `오브젝트 파일` 을 생성할때 전처리 과정을 거침
이때 `include`된 소스파일은 그냥 그자리에 붙여넣기됨

모듈화로 인해 여러 소스에서 필요한 라이브러리 소스가 있다고 치고 그게 막 10000줄 이렇게 넘어간다 치면 속도가 미쳐버리는거임
또 이렇게 여러번 복사되버리면 중복선언으로 인해 링킹 과정에서 또 애러날꺼임
(a->b->c 이런 참조관계인 상태에서 a만 컴파일 하면 문제가 없는데, 요즘 빌드시스템 들은 a, b, c 를 각각 빌드하고 합치는 경우가 많음)

하지만 해더파일을 통해 인터페이스만 선언한 코드를 `include`한다면 컴파일 속도가 확실히 빨라 질꺼임

### 관행

C/C++ 나온 연도를 생각해보자 당시 컴퓨터 사양으로 대규모 프로젝트를 한다고 하면
위에 말했던것 처럼 컴파일 속도 감당이 될까?
그래서 구현부만 따로 빼는 방식으로 오래전 부터 코딩 했었고 그게 관행으로 굳은점도 있다

## (Class) friend 키워드

```c++
friend void friedTest(Obj& target);
```

전역 함수에서 객체 클래스에 선언하여 접근 가능 하도록 함
객체를 인자로 받는경우 해당 객체에 private 인스턴스에 접근하기위해
해당 함수를 friend로 클래스에 등록 해서 쓰는 방식
다만 접근 권한만 가지는거지 해당 함수가 클래스 맴버에 등록되는게 아님

```c++
Person& operator*(double mul);
Person& operator*(double mul, const Person& origin); //이렇게 선언 하면 안됨
friend Person& operator*(double mul, Person& target);
```

friend가 가장 필요할때가 operator 때문인데 operator 오버로딩은 그 특성상
매개변수를 자기 멋대로 바꾸지 못함
하지만 예를들어 `target * 3` 이렇게 된다면 정상적으로 `operator*(double mul)` 함수가 수행 할꺼임
연산자 자체가 왼쪽 피연산자인 target 이 객체를 호출해서 인자를 `double mul`로 사용하게됨

그런데 3 _ target 이러면 어떻게 되는가 3이 `operator_`을 호출하게 되는데 이때 인자는`target`이 된다
그럼 `operator\*(double mul)`이거로는 호출 오류가 난다,
아까 말했다시피 operator오버로딩은 인자를 수정하기 어렵다.
그러면 이걸 그냥 전역 변수로 빼가지고 이렇게 선언하면 되는데, 문제는 `target`에 내부 private인스턴스 접근이 안된다.
이떄 쓰는게 friend 인것이다 (만약 public인스턴스만 접근할꺼라면 해당 선언 자체가 필요없음)

```c++
friend class Car;
```

만약 두 클래스가 밀접한 관계, Car객체 메소드 측에 Person을 인자로 받는경우
Person에 private에 접근할때 아예 클래스 자체를 friend로 선언 가능

## 매크로

https://wikidocs.net/86264
https://hyun-tori.tistory.com/15

### 매크로 함수 사용이유

- 컴파일 시 활성화할 수 있는 선택적 기능을 추가하고 싶을 때
- 다른 대상(윈도우, 리눅스, 맥 등)에 대해 다른 코드 경로를 컴파일하고 싶을 때
- 어떤 표현식을 인라인하고 싶을 때
- 시스템이 리틀 엔디안인지 빅 엔디안인지 확인하고 싶을 때 (비트 시프트를 할 때 중요함)
- void 포인터를 사용하는 대신 일반 함수를 작성하고 싶을 때 (타입 안전성이 떨어지고 잘못하면 오버플로우가 발생할 수 있음)
- 상수 값을 만들고 싶을 때 (C에서 const는 실제로 const가 아니며, 할당 후 값을 변경할 수 없다는 의미일 뿐임)
- 함수에 대한 더 간단한 API를 제공하는 별칭을 만들고 싶을 때

## 가상함수 테이블

C++에서는 가상 함수의 정의와 동작 방식만을 규정하고 있으며, 그에 따른 구현은 컴파일러마다 다르다.
대게 가상함수 테이블을 통해 관리된다.

C++ 컴파일러는 각각의 객체마다 가상 함수 테이블을 가리키는 포인터를 저장하기 위한 숨겨진 멤버를 하나씩 추가한다.
이와 함께 가상 함수를 단 하나라도 가지는 클래스에 대해서 가상함수 테이블이 작성된다.

이렇게 작성된 가상 함수 테이블에는 해당 클래스의 객체들을 위해 선언된 가상 함수들의 주소가 저장되게 된다.
가상 함수를 호출하면, C++ 프로그램은 가상 함수 테이블에 접근하여 자신이 필요한 함수의 주소를 찾아 호출하게 된다.
가상 함수를 사용하면 이처럼 함수의 호출 과정이 복잡해지므로, 메모리와 실행 속도 측면에서 약간의 부담을 가지게 됩니다.
따라서 C++에서 기본 바인딩은 정적 바인딩이며, 동적바인딩 필요한 경우에만 가상 함수로 선언하자.

## 일반화 프로그래밍

데이터를 중시하는 객체 지향 프로그래밍과는 달리 프로그램의 알고리즘에 그 중점을 두는 방식.
데이터 타입에 구애받지 않고 재사용 할 수있는 코드를 작성하는 프로그래밍
이러한 일반화 프로그래밍을 지원하는 대표적인 기능이 템플릿(template)임


STL 라이브러리가 일반화 프로그래밍으로 구현 되어있다.
즉 STL 라이브러리를 쓰는 순간 일반화 프로그래밍에 도움 받는다는 뜻

템플릿을 활용하여 직접 일반화 프로그래밍 기법을 적용하는게 매우 까다롭고 중급 개발자
수준에서는 디버깅 하기가 매우 까다롭다고 한다.

### 템플릿

매개변수의 타입에 따라 함수나 클래스를 생성하는 메커니즘.
템플릿은 타입이 매개변수에 의해 표현되므로, 매개변수화 타입(parameterized type)이라고도 불림
템플릿을 사용하면 타입마다 별도의 함수나 클래스를 만들지 않고, 여러 타입에서 동작할 수 있는 하나의 함수나 클래스를 작성하는 것이 가능함

## 포인터에 확실한 개념

- `참조 (&var)`: 그 변수를 저장하고 있는 주소를 **가르킨다**
- `역참조 (*var)`: 그 주소를 통해 **접근한다**:
    - 해당 주소에 있는 변수를 `불러온다`이렇게만 생각하면 안됨
    - 가령 `*a = *b` 라고 쳤을때 `*a` 변수 입장에서는 해당 주소에 쓰기 접근을 하는것이고, `*b` 입장에서는  
       해당 주소에 읽기 접근을 하는것
      즉 헤당 코드에 대해 `*a` 와 `*b`는 다른 동작을 하므로 어셈블리가 다르다

특정 변수에 대한 포인터참조를 바꾸고 싶다면 이중 포인터를 싸야한다

## 스마트 포인터

참고자료:

- [스마트 포인터에 대해 알아보자(1)](https://woo-dev.tistory.com/110)
- [스마트 포인터에 대해 알아보자(2)](https://woo-dev.tistory.com/111)
- [스마트 포인터에 대해 알아보자(3)](https://woo-dev.tistory.com/113)
- [스마트 포인터에 대한 다양한 의견들](https://www.reddit.com/r/cpp_questions/comments/1qhidty/what_are_the_best_practices_for_using_smart/?tl=ko)

골치아픈 메모리 관리를 좀더 수월하게 할 수있음
기존의 포인터는`원시 포인터`라 칭함

### 종류와 사용법

#### unique_ptr

> 하나의 스마트 포인터만이 특정 객체를 소유할 수 있도록
> 객체에 소유권을 도입, 즉 여러 포인터가 중복 접근하는 것을 막음

```cpp
unique_ptr<Person> safePtr = make_unique<Person>(10, "아사람", 50);

// nPtr에 소유권 이전 (복사는 불가함)
unique_ptr<Person> nPtr = move(safePtr);

// 메모리 영역삭제 (사실 자동으로 스코프의 의해 해제 하므로 필요 없음)
uPtr.reset();

// 원시 포인터 반환 (다만 소유하고 있는 채로 반환 하는거라 delete 해버리면 답없음)
uPtr.get();

// 원시 포인터로 내보내기 (소유권 포기 및 그냥 원시포인터로 사용)
uPtr.release();
```

소유권 이전이 되면 이전 객체포인터는 dangling pointer라고함, 절때 참조하면 안되는 포인터임

#### shared_ptr

> 레퍼런스 카운트를 이용하는 방법 특정 객체를 참조하고 있는 스마트 카운터가
총 몇개인지 파악하고 0이 되면 해제 하는 방식

```cpp
shared_ptr<SmartString> shaString = make_shared<SmartString>("시간이 지나도");
cout << "원시 객체 참조전 count: " << shaString.use_count() << endl;
// 원시 객체 참조랑 상관없이 최초 생성시 카운터는 무조건 1임
HeapTest* shTs = new HeapTest(&shaString->str, 20);
cout << "원시 객체 참조 후 count: " << shaString.use_count() << endl;
```

shared_ptr에 경우에도 카운터 증,감 원칙은 unique_ptr과 하다
최초 생성시 `shaString`이라는 스택 변수가 참조 하므로 카운터는 1이고, 해당 변수 소멸되면 자연스럽게 1이 감소한다.

이게 스마트 포인터가 원시포인터를 참조하는 관계이지만
원시포인터 입장에서는 스마트 포인터를 참조 하는 관계가 아니다.
따라서 실제 원시포인터를 참조하는 객체가 외부 에서 소멸되면, shared_ptr에 카운터가 감소하지 못함
이러한 관계 때문에 예를들어 같은 원시 포인터로 shared_ptr를 2개 이상 만든 경우
두 shared_ptr모두 카운트가 0이 되면 이중 해제 될 가능성이 존재함

```cpp
class ClassA {
   private:
    shared_ptr<ClassB> ptr;

   public:
    string name;
    void setClassB(shared_ptr<ClassB> ptr) { this->ptr = ptr; };
    ClassA(string name) { this->name = name; }
};
class ClassB {
   private:
    shared_ptr<ClassA> ptr;

   public:
    void setClassA(shared_ptr<ClassA> ptr) { this->ptr = ptr; };
};
/* A->B, B->A 참조 관계 */
```

shared_ptr은 기본적으로 strong ref(강한 참조) 방식이다.
카운트가 0이 되야 소멸되고, 0이 아니면 무조건 소멸하지 못한다.
이로인해 발생하는 문제가 **순환참조** 이다.
두 클래스에 shared_ptr로 선언된 멤버 변수가 서로가 서로를 참조하는 관계가 되버리면
서로 참조하기 때문에 영원히 카운트 1이 사라지질 못한다.

#### weak_ptr

> shared_ptr에 순환 참조 문제를 해결하기 위해 약한 참조 개념을 도입하여 사용
shared_ptr과 같이 사용되야함

shared_ptr은 단독 사용이 불가하고 shared_ptr 객체를 참조하는 방식으로 사용이 됨

weak_ptr이 shared_ptr을 참조 하면 약한 참조(Weak reference) 레퍼런스가 1 상승함

Weak reference는 메모리 헤제 하고는 관련이 없다. 횟수가 100이든 0이든 상관없고
만일 강한 참조가 0이되면 메모리 해제가 되면서 weak_ptr에 상태는 `expired` 가 되는데.
이 의미는 더이상 유요하지 않다 라는 뜻이다.

```cpp
shared_ptr<Person> shPtr_weak = make_shared<Person>(10, "언젱가", 50);
weak_ptr<Person> weakPtr = shPtr_weak;  // 내부적으로 shared_ptr에 원시 포인터를 참조
```

lockWeak을 통해 원시 값에 접근을 하려면 `lock()`을 통해 shared_ptr로 변환 해야 한다, 이때 shared_ptr에 강한 참조 카운팅 1이 상승한다.

```cpp
// shared_ptr로 형변환 하고, 원시 포인터를 가르킴 즉 강한 참조 카운팅이 1 상승
// 작업중에 다른 공유포인터가 소멸되더라도 강한참조 카운팅 1은 최소 확보 하므로 안전하게 사용가능
auto lockWeak = weakPtr.lock();
cout << lockWeak->getName() << endl;
cout << "count: " << weakPtr.use_count() << endl;
```

약한 참조를 사용하는 이유는 아까 shared_ptr에 순환 참조 문제를 해결 하는데 요긴하다.

예를들어 강한 참조를 하고 있는 두 객체 중 하나를 weak_ptr을 통해 참조 하게 한다면, 둘중 하나가 강한참조 카운팅이 0이 되면 객체가 지워질꺼고 그 순간
약한 참조를 하고 있던 객체도 expired 되어 자연스럽게 해제될 것이다.

### 메모리 참조 해제 시점 & 카운팅 감소 시점

> 기본적으로 `RAII` 원칙을 따른다(소유자의 생명주기와 정확히 일치)

- 스코프를 벗어 날때

    ```cpp
    {
      unique_ptr<Foo> p = make_unique<Foo>();
      // 블록 벗어나면 해제
    }
    ```

- `reset()` 호출시
- `nullptr` 대입시

    ```cpp
      unique_ptr<Foo> p = make_unique<Foo>();
      p = nullptr
    ```

- 예외가 발생하여 참조하고 있던 스택이 풀릴 시
- 더이상 참조할 수 있는 영역이 없거나 부모가 죽을 시

#### 내부 스마트포인터 해제 (부모가 해제될 시)

> 클래스 내부에 스마트포인터 참조 객체가 있을시

    해당 클래스가 힙으로 선언된 경우 클래스를 delete한 경우 연쇄적으로 제거 된다.

클래스는 스마트 포인터 이지만 해당 클래스 내부에 원시포인터 힙이있는경우
연쇄재거는 기대할 수 없다.

```cpp
// 클래스 내부에 힙 영역으로 선언한 변수가(원시 포인터) 있는경우
// 이 경우엔 여전히 스마트 포인터를 쓰더라도 해제 되지 못함
string* str = new string("그러나");
unique_ptr<HeapTest> htu = make_unique<HeapTest>(str, 10);
htu.reset();
cout << *str << endl;
```

이 경우에도 함수 인자로 전달할 변수를 `unique_ptr`로 선언하였지만
`HeapTest`에 전달된 변수는 `unique_ptr`이 가르키는 원시 포인터가 전달 되었다.
이렇게 되면 부모에 소멸과 별개로 `unique_ptr`가진 스코프 규칙에 따라 소멸 된다.
`unString`에 해제 시점은 블록을 벗어나야 된다.

```cpp
// 내부 맴버를 스마트포인터로
unique_ptr<string> unString = make_unique<string>("그러나");
// 하지만 클래스 내부 맴버에 원시포인터를 전달함 이경우 스마트 포인터 효과를 기대 불가
HeapTest* hts = new HeapTest(unString.get(), 10);
delete hts;
```

이렇게 내부 인자를 스마트 포인터로 해야 부모 해제시 연쇠적인 해제가 가능하다

```cpp
unique_ptr<SmartString> Shstr = make_unique<SmartString>("안녕 디지몬");
unique_ptr<SmartHeapTest> smHeap = make_unique<SmartHeapTest>(move(Shstr), 10);
smHeap.reset();

// 부모는 원시 포인터여도 가능 하다
SmartHeapTest* smHeap = new SmartHeapTest(move(Shstr), 10);
delete smHeap;
```

### make_unique & make_shared 사용 이유

스마트 포인터 초기화는 make 함수 말고 그냥 new 를 사용해도 가능함

```cpp
unique_ptr<int> uPtr(new int(5));
```

하지만 여러 이점 떄문에 make를 씀

1. 예외 안전성

`ptr` 클래스는 내부적으로 힙 할당이 이뤄짐, 만약 초기화를 객체의 new를 통해 한다면 또 그 객체에 대한 할당이 이뤄 질꺼임

`unique_ptr<int> uPtr(new int(5));` 만약 new int(5)는 정상적으로 할당 되었으나 unique_ptr이 메모리 부족으로 할당이 안됬다면?
그대로 메모리 누수나는거임

2. 원시 포인터 혼용 사용 방지

만약 `new int(5)`로 할당한 포인터를 다른곳에서 참조가 가능하다면, 코딩 시 원시포인터를 참조하는 실수를 할 수 있음

이래서 아예 원시포인터를 노출하지 않는 방법이 관례화 되어있음

### 스마트 포인터 단점

- 원시 포인터 보다 성능 저하가 심하다
  (스마트 포인터를 생성할때 드는 비용이 원시 포인터 대비 10배정도 높다)
- 포인터에 들어가는 메모리 크기가 늘어난다.
  (64비트 기준 원시 포인터는 8바이트 이지만 shared_ptr에 경우 16바이트 임)

### 적절한 사용처

- 성능이 매우 중요한 상황인경우 **원시포인터**
- 포인터 구조가 단순하게 나오는 경우 **원시 포인터**
- 메모리 해제 할당이 빈번한 포인터 변수에 경우 **원시 포인터**

즉 공유해서 쓰는 상황이 아닌경우 원시포인터를 쓰고, 아니면 대체로 스마트 포인터가 더 좋을꺼 같다.

## STL(Standard Template Library)

C++에 표준화된 템플릿 라이브러리

본래는 C++에 표준 라이브러리와 구분하였지만
현재는 표준 라이브러리에 흡수 되어서
표준 라이브러리중 Template에 의존하는 라이브러리들 이라고 할 수 있다.

표준 라이브러리라 함은 `<iostream>`같이 .h 없이 쓸 수 있는 라이브러리다.

> [나무위키 표준 라이브러리 목록 참고](https://namu.wiki/w/C%2B%2B/%ED%91%9C%EC%A4%80%20%EB%9D%BC%EC%9D%B4%EB%B8%8C%EB%9F%AC%EB%A6%AC#s-2)

Template 라이브러리는 일반화 프로그래밍 패러다임을 채택하여
일반화 프로그래밍에 한 축을 담당하고 있다.

### STL 구성요소

#### 컨테이너(container)

같은 타입의 여러 객체를 저장하는 일종의 집합
컨테이너는 클래스 템플릿으로, 컨테이너 변수를 선언할 때 컨테이너에 포함할 요소의 타입을 명시가능

#### 반복자(iterator)

STL 컨테이너에 저장된 요소를 반복적으로 순회하여, 각각의 요소에 대한 접근을 제공하는 객체
즉, 컨테이너의 구조나 요소의 타입과는 상관없이 컨테이너에 저장된 데이터를 순회하는 과정을 일반화한 표현
템플릿이 타입과 상관없이 알고리즘을 표현할 수 있게 해준다면, 반복자는 컨테이너와 상관없이 알고리즘을 표현하게 해주는것

클래스 내부를 포인터 처럼 구조가 짜여있어 포인터 처럼 사용하는 객체라 볼 수 있는데
한마디로 반복자는 컨테이너에 있는 모든 원소들을 읽을 때 사용하는 포인터 비슷한 객체다.

컨테이너에서 이뤄지는 모든 +,-,= 등의 연산은 이 반복자가 담당한다.

> java에 컬렉션 라이브러리에 `iterator` 생각해도 되지만 그 개념보다 훨씬 큰 집합이다.

반복자가 가져야할 요구사항

- 가리키는 요소의 값에 접근할 수 있어야 함. 따라서 참조 연산자(\*)가 정의되어야
- 반복자 사이의 대입 연산, 비교 연산이 가능해야 함 따라서 대입, 관계 연산자가 정의되어야
- 가리키는 요소의 주변 요소로 이동할 수 있어야 함 따라서 증가 연산자(++)가 정의되어야

##### 반복자 종류

| 반복자의 기능             | 입력 | 출력 | 순방향 | 양방향 | 임의 접근 |
| ------------------------- | ---- | ---- | ------ | ------ | --------- |
| 접근(->)                  | ○    | X    | ○      | ○      | ○         |
| 읽기(\*)                  | ○    | X    | ○      | ○      | ○         |
| 쓰기(\*)                  | X    | ○    | ○      | ○      | ○         |
| 증가 연산자(++)           | ○    | ○    | ○      | ○      | ○         |
| 감소 연산자(--)           | X    | X    | X      | ○      | ○         |
| 첨자 연산자([])           | X    | X    | X      | X      | ○         |
| 산술 연산자(+, -)         | X    | X    | X      | X      | ○         |
| 산술 대입 연산자(+=, -=)  | X    | X    | X      | X      | ○         |
| 관계 연산자(<, <=, >, >=) | X    | X    | X      | X      | ○         |

즉 반복자는 일종의 프로토콜 이다. 입력 반복자를 구현 하고 싶다.
그러면 접근, 읽기, 쓰기 연산자만 구현 하면된다.

#### 알고리즘(algorithm)

> sort, binary_search, lower_bound등 진짜 알고리즘을 뜻함

- 읽기 알고리즘

    > 데이터를 읽을 수 만있음
    - `find()`, `for_each()` 등

- 변경 알고리즘

    > 컨테이너 자체는 변경하지 않고, 컨테이너 지정 범위 내 요소만 변경
    - `copy()`, `swap()`, `transform()` 등

- 정렬 알고리즘

    > 정렬되도록 변경하는 함수
    - `sort()`, `stable_sort()`, `binary_search()` 등

- 수치 알고리즘

    > 수치 함수
    - `accumulate()`

### 함수 객체

객체변수 자체를 호출연산자() 를 사용하는 객체를 의미
펑크터(functor) 라고도 불림

해당 클래스에서 호출연산자() 를 오버로딩 하여 구현 함

#### 미리 정의된 함수 객체

| 함수 객체    | 연산자 | 함수 객체     | 연산자 |
| ------------ | ------ | ------------- | ------ |
| plus         | +      | greater       | >      |
| minus        | -      | greater_equal | >=     |
| multiplies   | \*     | less          | <      |
| divides      | /      | less_equal    | <=     |
| modulus      | %      | logical_and   | &&     |
| negate       | -      | logical_or    | \|\|   |
| equal_to     | ==     | logical_not   | !      |
| not_equal_to | !=     |               |        |

## 프로젝트 구성 시 C vs C++

> 핵심은 추상화 비용을 감수 할 가치가 있나?

C++ 은 멀티패러다임을 지원하면서 C 대비 추상화로 인한 오버해드가 생김
극도로 성능을 추구해야하는경우 C++ 보다 C가 더 나은 선택임

커널/드라이버 쪽은 `C`
그 위에 애플리케이션/미들웨어는 `C++`

### C를 사용해야 되는 경우

#### 리눅스 커널쪽 프로그래밍

리눅스는 순수 C로 만들어진 OS임 C++을 쓸 수는 있지만
별로 좋은 선택은 아님

#### 다른 언어와 혼용 사용 목적인 경우

C++은 클래스 레이아웃 이런게 컴파일러 마다 달라
혼용하려는 언어가 해당 레이아웃이 아닌경우 복잡해짐

반면 C는 ABI(Application Binary Interface)가 표준화 되있어 여러 언어와 상호작용 하기가 편함

만일 C++을 다른 언어에서 사용 하고 싶다면
라이브러리를 C ABI로 감싸고 구현을 C++로 함
(해더를 extern "C" 로 작성, 소스파일은 C++ 로 작성)

### C++을 사용해야 되는 경우

#### 복잡한 대이터 구조를 다루는경우

C++ 은 클래스를 비롯 객체지향, 일반화 프로그래밍 등을 지원하여 구조적 문제를 해결하기 편함

또한 C에서는 메모리 관리를 malloc-free 구조로 이루어 지지만, C++에서는 스마트 포인터와, 소멸자에 도입으로 관리가 더 편함

#### 대규모 애플리케이션 개발인 경우

게임이나, 브라우저 등 이미 수 많은 기반들이 C++로 코딩되있는 경우가 많아
어쩔 수 없이 C를 못쓰고 C++을 써야 되는 상황이 많음

## 추가적인 스터디

- 일반화 프로그래밍
- modern c++ 문법
- 생산자-소비자 패턴 (멀티 쓰레딩)
- (아주 나중에) 메모리 관리
